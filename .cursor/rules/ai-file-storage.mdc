---
description: 峰志指定的存檔位置與開資料夾規則
alwaysApply: true
---

# AI 轉檔／存檔位置

使用者請存檔時，必須先開立對應資料夾，再把檔案放進去。不要把檔案散放在專案根目錄。

## 本機／NAS 對應路徑

- 網路磁碟：`\\100.97.127.26\AI轉檔存放區\`
- 若 Y 槽已對應此分享：`Y:\AI轉檔存放區\`
- Repo 內鏡像資料夾：`AI轉檔存放區/`

雲端環境若寫不到 `\\100.97.127.26\`，仍要在 repo 的 `AI轉檔存放區/` 建立**相同層級的子資料夾**，方便使用者整包複製到 NAS。

## 開資料夾規則

1. 根目錄固定為 `AI轉檔存放區`
2. 依文件用途再開一層（或數層）對應資料夾，例如：
   - 合約／同意書 → `AI轉檔存放區/合約書格式/`
3. 檔名用正體中文、用途清楚；同一份文件的 PDF、Word、HTML 放在同一子資料夾
4. 未使用的分類不要預先亂建；有新類型時再新開對應資料夾

## 目前已建立

- `AI轉檔存放區/合約書格式/`：房屋及土地出售價款分配同意書
