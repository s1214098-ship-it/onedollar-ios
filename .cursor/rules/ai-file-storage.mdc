---
description: 峰志指定的存檔位置與開資料夾規則
alwaysApply: true
---

# AI 轉檔／存檔位置

使用者請存檔時，必須先開立對應資料夾，再把檔案放進去。不要把檔案散放在專案根目錄。

## 本機／NAS 對應路徑（以截圖為準）

分享名稱是「火旺資料」，不是「AI轉檔存放區」。

- 分享根目錄：`\\100.97.127.26\火旺資料\`
- AI 存檔根目錄：`\\100.97.127.26\火旺資料\AI轉檔存放區\`
- 若 Y 槽已對應「火旺資料」：`Y:\AI轉檔存放區\`
- Repo 內鏡像：`AI轉檔存放區/`

分享根目錄裡原本就有一個 `合約書格式`，那是舊資料夾。AI 產出的合約要放在：

`\\100.97.127.26\火旺資料\AI轉檔存放區\合約書格式\`

雲端環境目前無法完成 SMB 寫入。請使用者在本機執行 `AI轉檔存放區/複製到NAS.ps1`，或手動複製。Repo 仍要維持相同資料夾結構。

## 開資料夾規則

1. 先在 `\\100.97.127.26\火旺資料\` 底下開 `AI轉檔存放區`（沒有就新建）
2. 再依文件用途開子資料夾，例如合約 → `AI轉檔存放區\合約書格式\`
3. 同一份文件的 PDF、Word、HTML 放在同一子資料夾
4. 未使用的分類不要預先亂建

## 目前已建立（repo 鏡像）

- `AI轉檔存放區/合約書格式/`：房屋及土地出售價款分配同意書
